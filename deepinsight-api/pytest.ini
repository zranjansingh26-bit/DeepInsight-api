[pytest]
testpaths = tests
asyncio_mode = auto
python_files = test_*.py
python_functions = test_*
markers =
    unit: Unit tests (engines, pure logic)
    integration: Integration tests (API, database)
addopts = -v --tb=short
